function racePodium(blocks) {
  if (blocks === 7) {
    return [2, 4, 1]
  } else {
  const second = Math.ceil(blocks / 3);
  const first = second + 1;
  const third = blocks - second - first;
  return [second, first,  third];
  }
}
